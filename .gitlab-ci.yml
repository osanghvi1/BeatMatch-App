stages:
  - mavenbuild
  - maventest
  
maven-build:            
  stage: mavenbuild
  tags:
    - om_tag      # <-- change to your runner's tag
  script:
    - cd Backend/demo       # change 'Backend' to to where you have the pom.xml (do not add / in the beginning)
    - mvn package
  only:
    refs:
      - OM_CICD
    changes:
      - Backend/**/*


maven-test:             
  stage: maventest
  tags:
    - om_tag     # <-- change to your runner's tag
  script:
    - cd Backend/demo       # change 'Backend' to to where you have the pom.xml (do not add / in the beginning)
    - mvn test
  only:
    refs:
      - OM_CICD
    changes:
      - Backend/**/*
